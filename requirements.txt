# ──────────────────────────────────────────────────────────────
# cloud-ops-ai-agent — Production Dependencies
# ──────────────────────────────────────────────────────────────
# Pin exact versions for reproducible builds across environments.
# Python >=3.11 required (asyncio.TaskGroup, ExceptionGroup).
#
# Core runtime uses only the standard library (asyncio, json,
# logging, uuid, dataclasses, enum).  The packages below are
# needed when replacing mock stubs with real infrastructure.
# ──────────────────────────────────────────────────────────────

# Async HTTP client — real IdP / RAG endpoint calls in production
aiohttp==3.13.3

# AWS SDK — S3 log upload, Learner Lab session-token support
boto3==1.42.63

# Structured JSON-lines logging for ELK / Loki ingestion pipelines
python-json-logger==4.0.0
