# cloud-ops-ai-agent — Python dependencies
# Refactored based on Baidu Cloud-Phone scalable architecture patterns
#
# Core runtime: Python 3.11+ required for asyncio.TaskGroup and ExceptionGroup support.
# All standard-library modules (asyncio, json, logging, uuid, dataclasses, enum)
# need no external installation.

# HTTP client for real IdP/RAG endpoint calls (replace mock stubs in production)
aiohttp>=3.9.0

# Structured logging — optional drop-in for JSON-lines output in production
python-json-logger>=2.0.7

# Type checking support (development / CI only)
mypy>=1.9.0

# Linting
ruff>=0.4.0

# Unit testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
